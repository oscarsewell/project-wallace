[project]
name = "wallace"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Danny Bozbay", email = "107803920+dbozbay@users.noreply.github.com" }
]
requires-python = ">=3.14"
dependencies = [
    "bs4>=0.0.2",
    "requests>=2.33.1",
]

[project.scripts]
project-wallace = "wallace:main"

[build-system]
requires = ["uv_build>=0.11.8,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "ruff>=0.15.12",
]
